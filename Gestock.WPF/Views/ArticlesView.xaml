<UserControl x:Class="SuperMarcheApp.Views.ArticlesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F4F7F6">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Titre -->
            <RowDefinition Height="Auto"/>
            <!-- Formulaire (Carte) -->
            <RowDefinition Height="Auto"/>
            <!-- Barre d'actions -->
            <RowDefinition Height="*"/>
            <!-- Tableau -->
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Inventaire des Articles" FontSize="26" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Gérez vos produits, prix et niveaux de stock." Foreground="#7F8C8D" FontSize="13"/>
        </StackPanel>

        <!-- Formulaire dans une Carte Blanche -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" BorderBrush="#DCDDE1" BorderThickness="1">
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel Margin="10,5">
                    <TextBlock Text="Nom de l'article" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                    <TextBox Width="180" Height="32" VerticalContentAlignment="Center" BorderBrush="#DCDDE1"/>
                </StackPanel>

                <StackPanel Margin="10,5">
                    <TextBlock Text="Prix (€)" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                    <TextBox Width="100" Height="32" VerticalContentAlignment="Center" BorderBrush="#DCDDE1"/>
                </StackPanel>

                <StackPanel Margin="10,5">
                    <TextBlock Text="Catégorie" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                    <ComboBox Width="160" Height="32" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Boissons"/>
                        <ComboBoxItem Content="Produits laitiers"/>
                        <ComboBoxItem Content="Fruits et légumes"/>
                        <ComboBoxItem Content="Épicerie"/>
                        <ComboBoxItem Content="Hygiène"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="10,5">
                    <TextBlock Text="Stock" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                    <TextBox Width="80" Height="32" VerticalContentAlignment="Center" BorderBrush="#DCDDE1"/>
                </StackPanel>

                <StackPanel Margin="10,5">
                    <TextBlock Text="Date Expiration" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                    <DatePicker Width="150" Height="32" VerticalContentAlignment="Center"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- Barre d'outils / Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15">
            <Button Content="Enregistrer" Background="#27AE60" Foreground="White" Width="130" Height="35" Margin="5,0" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Modifier" Background="#2980B9" Foreground="White" Width="130" Height="35" Margin="5,0" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Supprimer" Background="#E74C3C" Foreground="White" Width="130" Height="35" Margin="5,0" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <!-- Liste des articles Stylisée -->
        <Border Grid.Row="3" Background="White" CornerRadius="10" BorderBrush="#DCDDE1" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Articles}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" GridLinesVisibility="Horizontal" 
                      HorizontalGridLinesBrush="#F1F2F6" BorderThickness="0"
                      Background="Transparent" RowHeight="35" FontSize="13">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F9FA"/>
                        <Setter Property="Foreground" Value="#34495E"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DCDDE1"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="80"/>
                    <DataGridTextColumn Header="Désignation" Binding="{Binding Nom}" Width="*"/>
                    <DataGridTextColumn Header="Catégorie" Binding="{Binding Categorie}" Width="150"/>
                    <DataGridTextColumn Header="Prix" Binding="{Binding Prix}" Width="100"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="100"/>
                    <DataGridTextColumn Header="Expiration" Binding="{Binding DateExpiration}" Width="130"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
