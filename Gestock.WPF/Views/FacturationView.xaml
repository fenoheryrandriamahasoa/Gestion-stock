<UserControl x:Class="SuperMarcheApp.Views.FacturationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             Background="#F4F7F6">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Titre -->
            <RowDefinition Height="Auto"/>
            <!-- Saisie Rapide -->
            <RowDefinition Height="*"/>
            <!-- Panier / Facture -->
            <RowDefinition Height="Auto"/>
            <!-- Total et Validation -->
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Caisse &amp; Facturation" FontSize="26" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Émission de nouvelles factures client." Foreground="#7F8C8D" FontSize="13"/>
        </StackPanel>

        <!-- Zone de Saisie Rapide -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" BorderBrush="#DCDDE1" BorderThickness="1" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Sélectionner l'article" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox Width="300" Height="35" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Quantité" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Width="80" Height="35" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Text="1"/>
                </StackPanel>

                <Button Content="+ Ajouter au panier" Background="#2980B9" Foreground="White" 
                        Width="180" Height="35" Margin="0,18,0,0" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <!-- Liste des articles facturés -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" BorderBrush="#DCDDE1" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Panier}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" GridLinesVisibility="Horizontal" 
                      HorizontalGridLinesBrush="#F1F2F6" BorderThickness="0"
                      Background="Transparent" RowHeight="40">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8F9FA"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{ Binding numero_ligne}" Width="50"/>
                    <DataGridTextColumn Header="Désignation" Binding="{Binding Nom}" Width="*"/>
                    <DataGridTextColumn Header="Prix Unit." Binding="{Binding Prix}" Width="120"/>
                    <DataGridTextColumn Header="Qté" Binding="{Binding Quantite}" Width="80"/>
                    <DataGridTextColumn Header="Sous-Total" Binding="{Binding TotalLigne}" Width="120"/>
                    <DataGridTemplateColumn Header="Actions" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <!-- Bouton Éditer -->
                                    <Button Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
						                    CommandParameter="{Binding}" Background="White" Foreground="Blue" Width="25" Height="25" 
                                            Margin="5,0" BorderThickness="0">
                                        <fa:IconBlock Icon="Pencil" FontSize="14" />
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                    <!-- Bouton Supprimer -->
                                    <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
						                    CommandParameter="{Binding}" Background="White" Foreground="Red" Width="25" Height="25" 
                                            Margin="5,0" BorderThickness="0">
                                        <fa:IconBlock Icon="Trash" FontSize="14"/>
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Zone de Total et Paiement -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Vider le panier" HorizontalAlignment="Left" 
                    Background="Transparent" Foreground="#E74C3C" BorderBrush="#E74C3C" 
                    Width="150" Height="40">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Border Background="#ECF0F1" Padding="20,10" CornerRadius="10,0,0,10" BorderBrush="#DCDDE1" BorderThickness="1,1,0,1">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="TOTAL À PAYER" FontSize="12" Foreground="#7F8C8D" HorizontalAlignment="Right"/>
                        <TextBlock Text="0,00 Ar" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Border>
                <Button Content="VALIDER &amp; IMPRIMER" Background="#27AE60" Foreground="White" 
                        Width="200" FontSize="14" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="0,10,10,0"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
