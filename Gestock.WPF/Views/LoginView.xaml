<Window x:Class="SuperMarcheApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Authentification - Gestion Supermarché"
        Width="600" Height="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        KeyDown="Window_KeyDown">

    <Border CornerRadius="15" Background="White"
            BorderBrush="#DCDDE1" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Panneau Gauche -->
            <Border Grid.Column="0" Background="#2C3E50"
                    CornerRadius="15,0,0,15">
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <Image Source="pack://application:,,,/Assets/shop.png"
                           Width="50" Height="50" Margin="5"/>
                    
                    <TextBlock x:Name="lblLoginStoreName"
                               Text="SUPERMARCHÉ"
                               Foreground="White" FontWeight="Bold"
                               FontSize="16" HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                    <TextBlock Text="Gestion de Stock"
                               Foreground="#BDC3C7" FontSize="10"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Panneau Droit -->
            <Grid Grid.Column="1" Margin="30">

                <!-- Bouton Fermer -->
                <Button Content="✕"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#BDC3C7"
                        FontSize="16"
                        Cursor="Hand"
                        Click="Button_Click"
                        Margin="0,-10,-10,0"/>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Connexion" FontSize="24"
                               FontWeight="Bold" Foreground="#2C3E50"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Veuillez vous identifier pour continuer"
                               Foreground="#7F8C8D" Margin="0,0,0,25"/>

                    <!-- Identifiant -->
                    <TextBlock Text="Identifiant"
                               Foreground="#34495E" FontSize="12"
                               FontWeight="SemiBold" Margin="0,0,0,5"/>

                    <TextBox x:Name="txtUsername"
                             Tag="Nom utilisateur"
                             Height="35"
                             Margin="0,0,0,15"/>

                    <!-- Mot de passe -->
                    <TextBlock Text="Mot de passe"
                               Foreground="#34495E" FontSize="12"
                               FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtPassword"
                                 Tag="Mot de passe"
                                 Height="35"
                                 Margin="0,0,0,10"/>

                    <!-- Bouton Connexion -->
                    <Button x:Name="Login"
                            Click="Login_Click"
                            Content="SE CONNECTER"
                            Height="40"
                            Margin="0,25,0,0"
                            Cursor="Hand"
                            Background="#27AE60"
                            Foreground="White"
                            FontWeight="Bold"
                            BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>