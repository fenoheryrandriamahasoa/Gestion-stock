<Window x:Class="SuperMarcheApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Authentification - Gestion Supermarché"
        Width="600" Height="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="15" Background="White" BorderBrush="#DCDDE1" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Panneau Gauche (Identité visuelle MainView) -->
            <Border Grid.Column="0" Background="#2C3E50" CornerRadius="15,0,0,15">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <!-- Remplacer shop.png par une icône blanche si possible -->
                    <Image Source="/Assets/shop.png" Width="80" Height="80" Margin="10"/>
                    <TextBlock Text="SUPERMARCHÉ" Foreground="White" FontWeight="Bold" 
                               FontSize="16" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <TextBlock Text="Gestion de Stock v2.0" Foreground="#BDC3C7" 
                               FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Panneau Droit (Formulaire) -->
            <Grid Grid.Column="1" Margin="30">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Bienvenue" FontSize="24" FontWeight="Bold" 
                               Foreground="#2C3E50" Margin="0,0,0,5"/>
                    <TextBlock Text="Veuillez vous identifier pour continuer" 
                               Foreground="#7F8C8D" Margin="0,0,0,25"/>

                    <!-- Pseudo -->
                    <TextBlock Text="Identifiant vendeur" Foreground="#34495E" 
                               FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Height="35" VerticalContentAlignment="Center" Padding="5,0"
                             BorderBrush="#DCDDE1" Margin="0,0,0,15">
                        <TextBox.Resources>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <!-- Mot de passe -->
                    <TextBlock Text="Mot de passe" Foreground="#34495E" 
                               FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <PasswordBox Height="35" VerticalContentAlignment="Center" Padding="5,0"
                                 BorderBrush="#DCDDE1">
                        <PasswordBox.Resources>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </PasswordBox.Resources>
                    </PasswordBox>

                    <!-- Bouton Connexion (Couleur rappelant le succès/chiffre d'affaire) -->
                    <Button Name="Login" Click="Login_Click" Content="SE CONNECTER"
                            Height="40" Margin="0,25,0,0" Cursor="Hand"
                            Background="#27AE60" Foreground="White" FontWeight="Bold" 
                            BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <!-- Bouton Fermer (Optionnel car WindowStyle=None) -->
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Background="Transparent" BorderThickness="0" Foreground="#BDC3C7" 
                        Click="Button_Click" Margin="0,-20,-20,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
